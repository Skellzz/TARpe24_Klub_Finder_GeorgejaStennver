@model Klub_Finder.Models.Jook

@{
    ViewData["Title"] = "Delete Drink";
}

<div class="container mt-5">
    <h1>Delete Drink</h1>

    <h4>Are you sure you want to delete this drink?</h4>

    <hr />

    <dl class="row">
        <dt class="col-sm-3">Drink Name</dt>
        <dd class="col-sm-9">@Model.DrinkName</dd>

        <dt class="col-sm-3">Drink Type</dt>
        <dd class="col-sm-9">@Model.DrinkType</dd>

        <dt class="col-sm-3">Alcohol %</dt>
        <dd class="col-sm-9">@Model.AlcoholPercentage</dd>

        <dt class="col-sm-3">Description</dt>
        <dd class="col-sm-9">@Model.Description</dd>

        <dt class="col-sm-3">Price</dt>
        <dd class="col-sm-9">@Model.Price €</dd>
    </dl>

    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-action="Index" class="btn btn-secondary">Cancel</a>
    </form>
</div>
